name: Build Mac Catalyst Release

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, reopened, synchronize, labeled]
    branches:
      - master
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-15
    timeout-minutes: 180
    if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'macOS') || github.event_name == 'workflow_dispatch'
    outputs:
      new_build_number: ${{ steps.generate_build_number.outputs.build_number }}
      project_version: ${{ steps.determine_marketing_version.outputs.project_version }}
      pkg_output_path: ${{ steps.build_app.outputs.pkg_output_path }}
      latest_commit_message: ${{ steps.get_latest_commit_details.outputs.commit_message }}
      branch_name: ${{ steps.get_latest_commit_details.outputs.branch_name }}
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      MATCH_READONLY: "true"

    steps:
      - name: Checkout Project
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Setup Caching
        uses: actions/cache@v3
        with:
          path: |
            ~/Library/Caches/CocoaPods
            ios/Pods
            ~/.npm
            node_modules
            vendor/bundle
          key: ${{ runner.os }}-catalyst-${{ hashFiles('**/package-lock.json', '**/Podfile.lock', '**/Gemfile.lock') }}
          restore-keys: |
            ${{ runner.os }}-catalyst-

      - name: Clear All Caches
        if: github.ref == 'refs/heads/master'
        run: |
          echo "Clearing Xcode DerivedData..."
          rm -rf ~/Library/Developer/Xcode/DerivedData
          echo "Clearing CocoaPods Cache..."
          rm -rf ~/Library/Caches/CocoaPods
          echo "Clearing npm Cache..."
          npm cache clean --force
          echo "Clearing Ruby Gems Cache..."
          rm -rf ~/.gem
          echo "Clearing Bundler Cache..."
          rm -rf ~/.bundle/cache

      - name: Ensure Correct Branch
        if: github.ref != 'refs/heads/master'
        run: |
          if [ -n "${GITHUB_HEAD_REF}" ]; then
            git fetch origin ${GITHUB_HEAD_REF}:${GITHUB_HEAD_REF}
            git checkout ${GITHUB_HEAD_REF}
          else
            git fetch origin ${GITHUB_REF##*/}:${GITHUB_REF##*/}
            git checkout ${GITHUB_REF##*/}
          fi
          echo "Checked out branch: $(git rev-parse --abbrev-ref HEAD)"

      - name: Get Latest Commit Details
        id: get_latest_commit_details
        run: |
          # Check if we are in a detached HEAD state
          if [ "$(git rev-parse --abbrev-ref HEAD)" == "HEAD" ]; then
            CURRENT_BRANCH=$(git show-ref --head -s HEAD | xargs -I {} git branch --contains {} | grep -v "detached" | head -n 1 | sed 's/^[* ]*//')
          else
            CURRENT_BRANCH=$(git rev-parse --abbrev-ref HEAD)
          fi
          
          LATEST_COMMIT_MESSAGE=$(git log -1 --pretty=format:"%s")
          
          echo "CURRENT_BRANCH=${CURRENT_BRANCH}" >> $GITHUB_ENV
          echo "LATEST_COMMIT_MESSAGE=${LATEST_COMMIT_MESSAGE}" >> $GITHUB_ENV
          echo "branch_name=${CURRENT_BRANCH}" >> $GITHUB_OUTPUT
          echo "commit_message=${LATEST_COMMIT_MESSAGE}" >> $GITHUB_OUTPUT

      - name: Specify Node.js Version
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest

      - name: Set Up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1.6
          bundler-cache: true

      - name: Install Dependencies with Bundler
        run: |
          bundle config path vendor/bundle
          bundle install --jobs 4 --retry 3 --quiet

      - name: Install Node Modules
        run: npm install --omit=dev --yes

      - name: Install CocoaPods Dependencies
        run: |
          bundle exec fastlane ios install_pods
          echo "CocoaPods dependencies installed successfully"

      - name: Update WWDR Certificate
        run: bundle exec fastlane ios update_wwdr_certificate

      - name: Generate Build Number Based on Timestamp
        id: generate_build_number
        run: |
          NEW_BUILD_NUMBER=$(date +%s)
          echo "NEW_BUILD_NUMBER=$NEW_BUILD_NUMBER" >> $GITHUB_ENV
          echo "build_number=$NEW_BUILD_NUMBER" >> $GITHUB_OUTPUT

      - name: Set Build Number
        run: bundle exec fastlane ios increment_build_number_lane

      - name: Determine Marketing Version
        id: determine_marketing_version
        run: |
          MARKETING_VERSION=$(grep MARKETING_VERSION BlueWallet.xcodeproj/project.pbxproj | awk -F '= ' '{print $2}' | tr -d ' ;' | head -1)
          echo "PROJECT_VERSION=$MARKETING_VERSION" >> $GITHUB_ENV
          echo "project_version=$MARKETING_VERSION" >> $GITHUB_OUTPUT
        working-directory: ios

      - name: Set Up Git Authentication
        env:
          ACCESS_TOKEN: ${{ secrets.GIT_ACCESS_TOKEN }}
        run: |
          git config --global credential.helper 'cache --timeout=3600'
          git config --global http.https://github.com/.extraheader "AUTHORIZATION: basic $(echo -n x-access-token:${ACCESS_TOKEN} | base64)"

      - name: Create Temporary Keychain
        run: bundle exec fastlane ios create_temp_keychain
        env:
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}

      - name: Create App Store Connect API Key JSON
        run: |
          # Create API key in an absolute path to avoid directory issues
          WORKSPACE_DIR="$(pwd)"
          API_KEY_PATH="${WORKSPACE_DIR}/appstore_api_key.json"
          
          echo "Creating API key at: ${API_KEY_PATH}"
          echo '${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}' > "${API_KEY_PATH}"
          
          # Make sure we set this for future steps
          echo "API_KEY_PATH=${API_KEY_PATH}" >> $GITHUB_ENV
          
          # Verify file was created
          if [ -f "${API_KEY_PATH}" ]; then
            echo "✅ API key file created successfully at ${API_KEY_PATH}"
            echo "File size: $(stat -f%z ${API_KEY_PATH}) bytes"
            chmod 600 "${API_KEY_PATH}"
          else
            echo "❌ Failed to create API key file at ${API_KEY_PATH}"
            exit 1
          fi

      - name: Setup App Store Connect API Key
        run: bundle exec fastlane ios setup_app_store_connect_api_key
        env:
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_PATH: "${{ env.API_KEY_PATH }}"
          ITC_TEAM_ID: ${{ secrets.ITC_TEAM_ID }}
          ITC_TEAM_NAME: ${{ secrets.ITC_TEAM_NAME }}

      # Replace complex setup with simpler match-based approach
      - name: Setup Provisioning Profiles for Mac Catalyst
        env:
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          GIT_ACCESS_TOKEN: ${{ secrets.GIT_ACCESS_TOKEN }}
          GIT_URL: ${{ secrets.GIT_URL }}
          ITC_TEAM_ID: ${{ secrets.ITC_TEAM_ID }}
          ITC_TEAM_NAME: ${{ secrets.ITC_TEAM_NAME }}
          KEYCHAIN_PASSWORD: ${{ secrets.KEYCHAIN_PASSWORD }}
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
          FASTLANE_PASSWORD: ${{ secrets.APPLE_ID_PASSWORD }}
        run: |
          echo "Setting up Mac Catalyst provisioning profiles from match repository..."
          
          # Only request Catalyst profiles for app identifiers that have them
          # From the error, we can see that only the main app and MarketWidget have Catalyst profiles
          
          # Retrieve main app profile first
          bundle exec fastlane run match \
            git_basic_authorization:"$GIT_ACCESS_TOKEN" \
            git_url:"$GIT_URL" \
            type:"appstore" \
            platform:"catalyst" \
            app_identifier:"io.bluewallet.bluewallet" \
            team_id:"$ITC_TEAM_ID" \
            team_name:"$ITC_TEAM_NAME" \
            readonly:"true" \
            keychain_name:"temp_keychain" \
            keychain_password:"$KEYCHAIN_PASSWORD" \
            username:"$APPLE_ID" \
            verbose:"true"
            
          # Then retrieve MarketWidget profile
          bundle exec fastlane run match \
            git_basic_authorization:"$GIT_ACCESS_TOKEN" \
            git_url:"$GIT_URL" \
            type:"appstore" \
            platform:"catalyst" \
            app_identifier:"io.bluewallet.bluewallet.MarketWidget" \
            team_id:"$ITC_TEAM_ID" \
            team_name:"$ITC_TEAM_NAME" \
            readonly:"true" \
            keychain_name:"temp_keychain" \
            keychain_password:"$KEYCHAIN_PASSWORD" \
            username:"$APPLE_ID" \
            verbose:"true"

      # Continue with the build step - no need for the custom lane
      - name: Build Mac Catalyst App
        id: build_app
        env:
          APPLE_ID: ${{ secrets.APPLE_ID }}
          APPLE_ID_PASSWORD: ${{ secrets.APPLE_PASSWORD }} # Note: Changed to use APPLE_PASSWORD
          FASTLANE_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${{ secrets.APPLE_PASSWORD }}
          MATCH_PASSWORD: ${{ secrets.MATCH_PASSWORD }}
          GIT_ACCESS_TOKEN: ${{ secrets.GIT_ACCESS_TOKEN }}
          GIT_URL: ${{ secrets.GIT_URL }}
          ITC_TEAM_ID: ${{ secrets.ITC_TEAM_ID }}
          ITC_TEAM_NAME: ${{ secrets.ITC_TEAM_NAME }}
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          APP_STORE_CONNECT_API_KEY_PATH: "${{ env.API_KEY_PATH }}"
          # Add environment variable to skip Bugsnag for Catalyst
          SKIP_BUGSNAG_CATALYST: 'YES'
        run: |
          # First ensure export_options_catalyst.plist has the correct team ID if not already set
          sed -i '' "s/A7W54YZ4WU/$ITC_TEAM_ID/g" ./ios/export_options_catalyst.plist
          cat ./ios/export_options_catalyst.plist
          
          # Verify project file integrity
          echo "Verifying Xcode project file integrity..."
          plutil -lint "./ios/BlueWallet.xcodeproj/project.pbxproj" || {
            echo "❌ Project file appears to be corrupt!"
            exit 1
          }
          
          # Now build the app - we've removed the disable/enable Bugsnag steps
          bundle exec fastlane ios build_catalyst_app

          # Ensure PKG path is set for subsequent steps
          if [ -f "./ios/build/pkg_path.txt" ]; then
            PKG_PATH=$(cat ./ios/build/pkg_path.txt)
            echo "PKG_OUTPUT_PATH=$PKG_PATH" >> $GITHUB_ENV
            echo "pkg_output_path=$PKG_PATH" >> $GITHUB_OUTPUT
            echo "Found PKG at: $PKG_PATH"
          else
            echo "Warning: pkg_path.txt not found, trying to locate PKG file manually..."
            PKG_PATH=$(find ./ios -name "*.pkg" | head -n 1)
            if [ -n "$PKG_PATH" ]; then
              echo "PKG_OUTPUT_PATH=$PKG_PATH" >> $GITHUB_ENV
              echo "pkg_output_path=$PKG_PATH" >> $GITHUB_OUTPUT
              echo "Found PKG at: $PKG_PATH"
            else
              echo "Error: No PKG file found"
              exit 1
            fi
          fi

      - name: Upload Mac Catalyst IPA as Artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: BlueWallet_MacCatalyst_AppStore_IPA
          path: ${{ env.PKG_OUTPUT_PATH }}
          retention-days: 7

      - name: Delete Temporary Keychain
        if: always()
        run: bundle exec fastlane ios delete_temp_keychain

  # Remove the notarize-macos job since it's not needed for TestFlight uploads
  # TestFlight handles the app validation and doesn't require notarization

  testflight-upload-macos:
    needs: [build]
    runs-on: macos-15
    if: github.event_name == 'push' || contains(github.event.pull_request.labels.*.name, 'testflight-macos')
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      NEW_BUILD_NUMBER: ${{ needs.build.outputs.new_build_number }}
      PROJECT_VERSION: ${{ needs.build.outputs.project_version }}
      LATEST_COMMIT_MESSAGE: ${{ needs.build.outputs.latest_commit_message }}
      BRANCH_NAME: ${{ needs.build.outputs.branch_name }}
    steps:
      - name: Checkout Project
        uses: actions/checkout@v4

      - name: Set Up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1.6
          bundler-cache: true

      - name: Install Dependencies with Bundler
        run: |
          bundle config path vendor/bundle
          bundle install --jobs 4 --retry 3 --quiet

      # Update to download the correct artifact name
      - name: Download Mac Catalyst IPA from Artifact
        uses: actions/download-artifact@v4
        with:
          name: BlueWallet_MacCatalyst_AppStore_IPA
          path: ./catalyst-build/

      - name: Create App Store Connect API Key JSON
        run: |
          # Create API key in an absolute path to avoid directory issues
          WORKSPACE_DIR="$(pwd)"
          API_KEY_PATH="${WORKSPACE_DIR}/appstore_api_key.json"
          
          echo "Creating API key at: ${API_KEY_PATH}"
          echo '${{ secrets.APP_STORE_CONNECT_API_KEY_CONTENT }}' > "${API_KEY_PATH}"
          
          # Make sure we set this for future steps
          echo "API_KEY_PATH=${API_KEY_PATH}" >> $GITHUB_ENV
          
          # Verify file was created
          if [ -f "${API_KEY_PATH}" ]; then
            echo "✅ API key file created successfully at ${API_KEY_PATH}"
            echo "File size: $(stat -f%z ${API_KEY_PATH}) bytes"
            chmod 600 "${API_KEY_PATH}"
          else
            echo "❌ Failed to create API key file at ${API_KEY_PATH}"
            exit 1
          fi

      - name: Find IPA and Set Path
        run: |
          # List downloaded artifacts
          echo "Downloaded artifacts:"
          find ./catalyst-build -type f | sort
          
          # Find IPA file in the downloaded artifact directory
          IPA_PATH=$(find ./catalyst-build -name "*.ipa" | head -n 1)
          
          if [ -n "$IPA_PATH" ]; then
            echo "Found IPA at: $IPA_PATH"
            echo "IPA_PATH=$IPA_PATH" >> $GITHUB_ENV
          else
            echo "❌ No IPA file found in the artifacts directory"
            exit 1
          fi

      - name: Verify IPA Path Before Upload
        run: |
          if [ ! -f "$IPA_PATH" ]; then
            echo "❌ IPA file not found at path: $IPA_PATH"
            ls -la ./catalyst-build/
            exit 1
          else
            echo "✅ Found IPA at: $IPA_PATH"
          fi

      - name: Upload Mac Catalyst App to TestFlight
        run: |
          echo "Uploading Mac Catalyst app to TestFlight via IPA format..."
          
          # Generate changelog
          branch_name="${{ env.BRANCH_NAME }}"
          commit_message="${{ env.LATEST_COMMIT_MESSAGE }}"
          version="${{ env.PROJECT_VERSION }}"
          build_number="${{ env.NEW_BUILD_NUMBER }}"
          
          changelog="Build Information:\n"
          
          # Include the branch name only if it is not 'master'
          if [ "$branch_name" != "master" ]; then
            changelog+="- Branch: $branch_name\n"
          fi
          
          changelog+="- Commit: $commit_message\n"
          changelog+="- Mac Catalyst Build"
          
          # Use fastlane to upload to TestFlight
          bundle exec fastlane run upload_to_testflight \
            api_key_path:"./appstore_api_key.json" \
            ipa:"$IPA_PATH" \
            skip_waiting_for_build_processing:true \
            changelog:"$changelog"
        env:
          FASTLANE_APPLE_APPLICATION_SPECIFIC_PASSWORD: ${{ secrets.APPLE_APP_SPECIFIC_PASSWORD }}
          APP_STORE_CONNECT_API_KEY_KEY_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_KEY_ID }}
          APP_STORE_CONNECT_API_KEY_ISSUER_ID: ${{ secrets.APP_STORE_CONNECT_API_KEY_ISSUER_ID }}
          ITC_TEAM_ID: ${{ secrets.ITC_TEAM_ID }}

  # Add this job for direct distribution support (optional)
  notarize-for-direct-distribution:
    needs: build
    runs-on: macos-15
    # Run this job only when explicitly requested with a label or parameter
    if: contains(github.event.pull_request.labels.*.name, 'direct-distribution') || github.event.inputs.direct_distribution == 'true'
    env:
      APPLE_ID: ${{ secrets.APPLE_ID }}
      NEW_BUILD_NUMBER: ${{ needs.build.outputs.new_build_number }}
      PROJECT_VERSION: ${{ needs.build.outputs.project_version }}
    steps:
      # ... steps for creating a PKG and notarizing it ...
      
      - name: Create PKG from IPA for Direct Distribution
        run: |
          echo "Converting IPA to PKG format for direct distribution..."
          # Steps to convert IPA to PKG would go here
          
      - name: Notarize PKG
        run: |
          echo "Notarizing PKG for direct distribution..."
          # Notarization steps would go here