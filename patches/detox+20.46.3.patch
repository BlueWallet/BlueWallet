diff --git a/node_modules/detox/src/devices/common/drivers/android/exec/ADB.js b/node_modules/detox/src/devices/common/drivers/android/exec/ADB.js
index 9a97f68..d2c1f6f 100644
--- a/node_modules/detox/src/devices/common/drivers/android/exec/ADB.js
+++ b/node_modules/detox/src/devices/common/drivers/android/exec/ADB.js
@@
   async pidof(deviceId, bundleId) {
     const bundleIdRegex = escape.inQuotedRegexp(bundleId) + '$';
-    const command = `ps | grep "${bundleIdRegex}"`;
+    const command = `pidof ${bundleId} 2>/dev/null || ps | grep "${bundleIdRegex}" || true`;
     const options = { silent: true };
 
     const processes = await this.shell(deviceId, command, options).catch(() => '');
     if (!processes) {
       return NaN;
     }
 
-    return parseInt(processes.split(' ').filter(Boolean)[1], 10);
+    const tokens = processes.split(' ').filter(Boolean);
+    const first = tokens[0];
+    const second = tokens[1];
+    const pid = Number.isFinite(Number(first)) ? first : second;
+
+    return parseInt(pid, 10);
   }