import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  navHeaderTitle: {
    fontSize: 22,
    fontWeight: '700',

    // TODO: Figure out how to get the nav title properly centered.
    // textAlign: 'center',
  },
});
