/**
 * 📝: More types to be supported in the future.
 */
export const OrderType = Object.freeze({
  LIMIT: 'Limit',
  MARKET: 'Market',
});

export const MarginType = Object.freeze({
  ISOLATED: 'Isolated',
  CROSS: 'Cross',
});
